{
  "name": "time-manager",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @time-manager/web dev",
    "build": "pnpm --filter @time-manager/web build",
    "start": "pnpm --filter @time-manager/web start",
    "lint": "pnpm --filter @time-manager/web lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
